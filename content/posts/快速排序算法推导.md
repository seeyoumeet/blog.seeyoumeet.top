---
title: "快速排序算法推导"
date: 2026-02-09T10:00:00+08:00
draft: false  # 注意：发布时要设为false，否则不会生成静态页
---

## 快速排序核心思想
快速排序基于分治思想，时间复杂度 $O(n\log n)$。

### 核心代码（Python）
```python
def quick_sort(arr):
    if len(arr) <= 1:
        return arr
    pivot = arr[0]
    left = [x for x in arr[1:] if x <= pivot]
    right = [x for x in arr[1:] if x > pivot]
    return quick_sort(left) + [pivot] + quick_sort(right)