# 核心构建配置
[build]
  command = "hugo --gc --minify" 
  publish = "public"

# 强制指定 Hugo 版本和环境（关键解决 unknown 问题）
[build.environment]
  HUGO_VERSION = "0.116.0"
  HUGO_EXTENDED = "true"
  HUGO_ENV = "production"
  NETLIFY_USE_HUGO = "true"

