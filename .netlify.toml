# 核心构建配置
[build]
  command = "hugo" 
  publish = "public"

# 强制指定 Hugo 版本和环境（关键解决 unknown 问题）
[build.environment]
  HUGO_VERSION = "0.128.0"
  HUGO_EXTENDED = "true"
  HUGO_ENV = "production"

# 可选：强制安装 Hugo 依赖（兜底）
[build.system]
  shell = "bash"